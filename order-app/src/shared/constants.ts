export enum ModelName {
  ORDER = 'Order',
}

export enum QueueName {
  ORDER = 'Order',
}

export enum ProcessorName {
  ORDER = 'Order',
}

export enum JobName {
  ORDER_DELIVERED = 'OrderDelivered',
}
